<nav class="navbar navbar-toggleable-md navbar-inverse bg-#00C488">
  <div class="container">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand" href="#">Fund it Forward</a>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <%= link_to "Projects", projects_path, class:'nav-link'%>
        </li>
      </ul>
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <%= link_to "Post A Project", new_project_path, class: 'btn btn-primary btn-margin-right'%>
          <%= link_to "Profile", profile_path(current_user.id), class: 'btn btn-primary btn-margin-right' %>
          <%= link_to "Log out", destroy_user_session_path, method: :delete, class: 'btn btn-danger my-2 my-sm-0' %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: 'btn btn-success my-2 my-sm-0 btn-margin-right' %>
          <%= link_to "Sign up", new_user_registration_path, class: 'btn btn-success my-2 my-sm-0' %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>